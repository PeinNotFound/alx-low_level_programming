#ifndef MAIN_H
#define MAIN_H

/* Headers*/
#include <stdio.h>
#include <string.h>

/* Prototypes */
unsigned int binary_to_uint(const char *b);
void print_binary(unsigned long int n);

#endif /* MAIN_H */
